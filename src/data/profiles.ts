import type { ProfileRow } from '../types';

// Placeholder — full implementation in Task 5.1.
// Returns null so AuthCallback routes new users to /welcome.
export async function getProfile(_userId: string): Promise<ProfileRow | null> {
  return null;
}
